interface:
  display_name: "电缆声纹数据链路"
  short_description: "校验数据中心 JSON、连接声纹数据库、查询待推理样本并返回前端展示数据"
  default_prompt: "Use $frontend-db-dashboard to validate a cable voiceprint JSON manifest, query the database, or return frontend sample display data."
