interface:
  display_name: "电缆声纹数据链路"
  short_description: "展示声纹数据库内容、校验数据中心 JSON、查询待推理样本并返回前端展示数据"
  default_prompt: "Use $frontend-db-dashboard to show cable voiceprint database contents, validate a JSON manifest, or return frontend sample display data."
