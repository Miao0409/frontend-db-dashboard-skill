interface:
  display_name: "Cable Voiceprint Data Pipeline"
  short_description: "Validate voiceprint manifests, query MySQL samples, run the bundled demo classifier, and submit algorithm results."
  default_prompt: "Use $frontend-db-dashboard to validate cable voiceprint JSON, query pending samples, run the demo fault-diagnosis algorithm, submit results, or return frontend sample display data."
